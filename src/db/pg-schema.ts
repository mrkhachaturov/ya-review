import type { DbClient } from './driver.js';

/** Initialize PostgreSQL schema with pgvector. Full implementation in Task 8. */
export async function initPgSchema(_db: DbClient): Promise<void> {
  throw new Error('PostgreSQL schema not yet implemented');
}
